# Installed packages — restored with "npm install", never stored in git
node_modules/

# Build output — regenerated by build-singlefile.mjs
dist/
dist-sf/

# Big binaries that don't belong in a code repo
*.mov
*.exe
*.zip
*.tar.gz

# OS clutter
Thumbs.db
.DS_Store
